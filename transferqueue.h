#ifndef TRANSFERQUEUE_H
#define TRANSFERQUEUE_H

class TransferQueue
{
public:
    TransferQueue();

    // add get request
    // add push request
    // addGetRequest(fileInfo, fileRepo, url)
    // addPushRequest(fileInfo, fileRepo, url)

    /*
      so we have a server, which we want to sync against

      */
};

#endif // TRANSFERQUEUE_H
